

void heapify(int arr[], int i, int n);

void buildmaxheap(int arr[], int n)
{
	int i = n/2;  
	for (i; i>0; i--)   //heapify from node n/2 down to node 1
	{
		heapify(arr, i-1, n-1); //matrix index is 1 smaller than size
	}

}
